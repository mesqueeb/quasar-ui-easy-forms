import merge from 'merge-anything'

const description = `This is an example of all the possible models for \`component: 'EfSelect'\`.`

export default {
  mode: 'edit',
  actionButtons: [],
  schema: [
    {
      component: 'QMarkdown',
      src: description,
    },
  ],
}
