const description = ``

export default {
  mode: 'edit',
  actionButtons: [],
  schema: [
    {
      component: 'QMarkdown', noContainer: true,
      noLineNumbers: true,
      src: description,
    },
  ],
}
